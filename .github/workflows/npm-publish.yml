name: Publish typescript-web-client
on:
  release:
    types: [created]
jobs:
  publish-npm:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
          registry-url: https://registry.npmjs.org/
      - run: npm publish
        working-directory: dist/typescript-web
        env:
          NODE_AUTH_TOKEN: ${{secrets.npm_token}}